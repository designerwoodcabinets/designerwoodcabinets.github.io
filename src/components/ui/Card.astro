---
import Icon from "@ui/Icon.astro";
import Line from "./Line.astro";

const {
    title,
    icon,
    description
} = Astro.props;
---

<div class="sm:flex items-start gap-4 w-full">
    {icon && (
        <Icon name={icon} className="w-40 h-16 mb-4"/>
    )}
    <div class="w-full">
        <h2 class="text-2xl font-medium pb-1 border-b-2 border-[var(--brand-gray-950)] sm:text-3xl">
            {title}
        </h2>
        <p class="text-base p-4 bg-[var(--brand-gray-50)] sm:text-lg">
            {description}
        </p>
    </div>
</div>